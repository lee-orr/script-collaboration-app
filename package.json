{
    "scripts": {
        "setup": "rustup target add wasm32-unknown-unknown && cargo install wasm-pack && cargo install rsw && cargo install trunk && cd frontend && npm install",
        "frontend": "cd ./frontend-ts && pnpm dev",
        "frontend-prod": "cd ./frontend-ts && pnpm build"
    }
}