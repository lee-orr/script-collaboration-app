{
    "scripts": {
        "backend": "cd ./backend && npx @cloudflare/wrangler dev",
        "setup": "rustup target add wasm32-unknown-unknown && cargo install trunk && cd frontend && npm install",
        "frontend": "cd ./frontend && npm run dev",
        "frontend-prod": "cd ./frontend && npm run prod"
    }
}